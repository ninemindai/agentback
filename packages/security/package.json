{
  "name": "@agentback/security",
  "version": "0.5.2",
  "description": "Security primitives (UserProfile, Subject, principals) — ported from @loopback/security",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentback/core": "workspace:~",
    "tslib": "^2.8.1"
  },
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "@agentback/testlab": "workspace:~",
    "vitest": "~4.1.9"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ninemindai/agentback.git",
    "directory": "packages/security"
  },
  "homepage": "https://agentback.dev",
  "bugs": "https://github.com/ninemindai/agentback/issues"
}
